eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9001/eureka/
spring:
  cloud:
    config:
      discovery:
       enabled: true
       serviceId: configserver
      name: configtest
      porfile: service
      label: master
